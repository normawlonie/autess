if (scaleType === ScaleType.POINT) {
    // Code to execute if scaleType is equal to ScaleType.POINT
    console.log('The scale type is POINT.');
} else {
    // Code to execute if scaleType is not equal to ScaleType.POINT
    console.log('The scale type is not POINT.');
}
